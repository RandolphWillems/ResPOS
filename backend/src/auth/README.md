# Auth

Nog te bouwen (Fase 1):
- POST /api/auth/login — email + wachtwoord, retourneert JWT
- Middleware `requireAuth` en `requirePermission(permissionName)`
- Gebruikers- en rollenbeheer (CRUD), zie Prisma-modellen User/Role/Permission
